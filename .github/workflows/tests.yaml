name: Unit Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "windows-latest", "macos-latest"]
        python-version: ["3.11", "3.12"]
        torch-channel: ["cpu"]   # keep CPU for CI stability
        torch-version: ["2.6.0", "2.7.0"]  # choose 1–2 versions

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python (with pip cache)
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip   # course-recommended caching :contentReference[oaicite:1]{index=1}

      - name: Upgrade pip tooling
        run: |
          python -m pip install -U pip setuptools wheel

      # IMPORTANT: install PyTorch explicitly (so you can test multiple versions)
      - name: Install PyTorch
        run: |
          python -m pip install torch==${{ matrix.torch-version }} torchvision torchaudio --index-url https://download.pytorch.org/whl/${{ matrix.torch-channel }}

      - name: Install project deps
        run: |
          python -m pip install -r requirements.txt

      - name: Run unit tests (exclude integration)
        run: |
          python -m pytest -m "not integration" --maxfail=1

      - name: Coverage
        run: |
          python -m pip install coverage
          coverage run -m pytest -m "not integration"
          coverage report -m
